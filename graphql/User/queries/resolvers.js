const User = require('../../../mongo/models/User');

module.exports.userResolver = async function (_, {id}, context) {
    try{

    }catch(error){
        console.error(error);
        throw(error);
    }
};